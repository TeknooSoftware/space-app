knpu_oauth2_client:
    clients:
        digital_ocean:
            type: 'digital_ocean'
            client_id: '%env(OAUTH_CLIENT_ID)%'
            client_secret: '%env(OAUTH_CLIENT_SECRET)%'
            redirect_route: space_connect_oauth_check
            redirect_params: {}
#       generic:
#           type: 'generic'
#           client_id: '%env(OAUTH_CLIENT_ID)%'
#           client_secret: '%env(OAUTH_CLIENT_SECRET)%'
#           redirect_route: space_connect_oauth_check
#           redirect_params: {}
#           provider_class: '%teknoo.space.oauth.provider.class%'
        github:
            type: 'github'
            client_id: '%env(OAUTH_CLIENT_ID)%'
            client_secret: '%env(OAUTH_CLIENT_SECRET)%'
            redirect_route: space_connect_oauth_check
            redirect_params: {}
        gitlab:
            type: 'gitlab'
            client_id: '%env(OAUTH_CLIENT_ID)%'
            client_secret: '%env(OAUTH_CLIENT_SECRET)%'
            redirect_route: space_connect_oauth_check
            redirect_params: {}
            domain: '%env(OAUTH_SERVER_URL)%'
        google:
            type: 'google'
            client_id: '%env(OAUTH_CLIENT_ID)%'
            client_secret: '%env(OAUTH_CLIENT_SECRET)%'
            redirect_route: space_connect_oauth_check
            redirect_params: {}
        jira:
            type: 'jira'
            client_id: '%env(OAUTH_CLIENT_ID)%'
            client_secret: '%env(OAUTH_CLIENT_SECRET)%'
            redirect_route: space_connect_oauth_check
            redirect_params: {}
        microsoft:
            type: 'microsoft'
            client_id: '%env(OAUTH_CLIENT_ID)%'
            client_secret: '%env(OAUTH_CLIENT_SECRET)%'
            redirect_route: space_connect_oauth_check
            redirect_params: {}
